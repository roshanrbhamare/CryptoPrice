<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Document</title>
</head>
<body>
    <div>
    <form action="/p" method="POST">
        <h1>Here you will get the current price of CryptoCurrency</h1>
        <h2>So type the Symbol of CryptoCurrency</h2>
        <input class="p" type="text" placeholder="Write the Code of CryptoCurrency BTC-for BitCoin" name="code">
        <button class="s" type="submit" value="Submit">+</button>
        <% if (locals.price) { %>
          <h1 id="re">Price of the <%= blockType %> is <%= price %> Dollars</h1>
        <% } %>
        <% if (locals.para) { %>
            <p><%= para %></p>
        <% } %>
        
    </form>
    </div>
</body>
</html>